const P=window.PORTFOLIO_PROJECTS||[],e=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const G=document.querySelector('#gallery');if(G)G.innerHTML=P.map(p=>`<a class="project-tile" href="project.html?id=${encodeURIComponent(p.id)}"><img src="${p.hero}" alt="${e(p.title)}" loading="lazy"><span class="project-overlay"><span>${e(p.title)}</span></span></a>`).join('');
const R=document.querySelector('#project-content');if(R){const q=new URLSearchParams(location.search).get('id');let i=P.findIndex(p=>p.id===q);if(i<0)i=0;const p=P[i],a=p.awards?.length?`<div class="awards">${p.awards.map(x=>`<p>${e(x)}</p>`).join('')}</div>`:'',o=p.overview?`<section class="project-copy"><h2>Overview</h2><p>${p.overview}</p>${p.externalLink?`<a class="text-link" href="${p.externalLink.url}" target="_blank" rel="noopener noreferrer">${e(p.externalLink.label)}</a>`:''}</section>`:'',v=p.video?.type==='youtube'?`<div class="video-wrapper"><iframe src="https://www.youtube-nocookie.com/embed/${p.video.id}" title="${e(p.title)} video" allowfullscreen loading="lazy"></iframe></div>`:p.video?.type==='vimeo'?`<div class="video-wrapper"><iframe src="https://player.vimeo.com/video/${p.video.id}${p.video.hash?`?h=${p.video.hash}`:''}" title="${e(p.title)} video" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen loading="lazy"></iframe></div>`:'',g=p.gallery?.length?`<section class="project-gallery">${p.gallery.map((x,j)=>`<img src="${x}" alt="${e(p.title)} supporting image ${j+1}" loading="lazy">`).join('')}</section>`:'';const pr=P[(i-1+P.length)%P.length],nx=P[(i+1)%P.length];R.innerHTML=`<header class="project-heading"><h1>${e(p.title)}</h1>${a}</header><img class="project-hero" src="${p.hero}" alt="${e(p.title)} hero image">${o}${v}${g}<nav class="project-nav"><a href="project.html?id=${pr.id}">Previous Project</a><a href="index.html">Back to Work</a><a href="project.html?id=${nx.id}">Next Project</a></nav>`;document.title=`${p.title} | Brittany Taylor`}
